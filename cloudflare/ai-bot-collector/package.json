{
  "name": "trustdata-ai-bot-collector",
  "version": "0.1.0",
  "description": "Cloudflare Worker that forwards zone HTTP logs to TrustData for AI-bot observability.",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20250129.0",
    "typescript": "^5.4.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
